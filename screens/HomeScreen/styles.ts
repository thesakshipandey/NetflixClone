import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        padding: 20,
    },
    
    image: {
        width: 100,
        height: 170,
        resizeMode: 'cover',
        borderRadius: 5,
    },
});

export default styles;